# @package _global_
optimizer:
  lr: 1.3e-4
  weight_decay: 1e-2

augmentation:
  mix: none

train:
  label_smoothing: 0.0
  class_weight_mode: balanced

scheduler:
  name: cosine

solve:
  candidate_name: top1
  candidate_hash: 100c8e1d42
